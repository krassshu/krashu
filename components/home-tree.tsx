import { t, type Locale } from '@/lib/i18n';
import { Icon } from './icons';
const nodes = [['person', 'Osoby', 'Właściciele i domownicy'], ['car', 'Pojazdy', 'Polisy, serwis, przeglądy'], ['file', 'Dokumenty', 'Informacje z kontekstem'], ['box', 'Urządzenia', 'Instrukcje i gwarancje'], ['network', 'Sieć', 'Infrastruktura domu'], ['calendar', 'Terminy', 'Rzeczy, o których pamiętasz'], ['plug', 'Integracje', 'Kolejne warstwy systemu']];
export function HomeTree({ locale = "pl" }: {
    locale?: Locale;
} = {}) { return <figure className="home-tree"><div className="panel-toolbar"><span><span className="status-dot"/>{t(" HOME / OBJECT TREE", locale)}</span><span>{t("model koncepcyjny", locale)}</span></div><div className="tree-body"><div className="tree-root"><span className="tree-root-icon"><Icon name="home" size={22}/></span><div><strong>{t("Dom", locale)}</strong><span>{t("Wszystko zaczyna si\u0119 tutaj.", locale)}</span></div><span className="mono tree-id">{t("root", locale)}</span></div><ul className="tree-nodes">{t(nodes.map(([icon, name, desc], i) => <li key={name}><span className="tree-branch"/><Icon name={icon} size={18}/><strong>{t(name, locale)}</strong><span className="tree-description">{t(desc, locale)}</span>{t(i === 6 && <span className="tag planned">{t("plan", locale)}</span>, locale)}</li>), locale)}</ul></div><figcaption><Icon name="box" size={14}/>{t(" Jeden model. Po\u0142\u0105czone informacje.", locale)}<span className="mono">{t("local://home", locale)}</span></figcaption></figure>; }
