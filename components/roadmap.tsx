import { t, type Locale } from '@/lib/i18n';
export const milestones = [{ version: '1.0', name: 'HomeOS Core', features: 'Dokumenty / Obiekty / Wyszukiwanie / Przypomnienia', description: 'Obecny zakres MVP: uporządkowanie dokumentów wokół obiektów i powiązanych terminów.', current: true }, { version: '1.x', name: 'Kalendarz', features: 'Calendar', description: 'Wspólny widok terminów. Rozwinięcie informacji zapisanych w Core.' }, { version: '2.x', name: 'Dom połączony', features: 'Home Assistant', description: 'Planowana integracja z Home Assistant. Połączenie modelu domu z jego urządzeniami.' }, { version: '3.x', name: 'Monitoring', features: 'Cameras / NVR', description: 'Planowana warstwa kamer i lokalnej rejestracji obrazu.' }, { version: '4.x', name: 'Energia', features: 'Energy', description: 'Planowane rozszerzenie o dane dotyczące energii w domu.' }, { version: '5.x', name: 'Lokalne AI', features: 'Local AI', description: 'Dalszy kierunek rozwoju: lokalna praca z kontekstem zgromadzonym w HomeOS.' }];
export function Roadmap({ detailed = false, locale = "pl" }: {
    detailed?: boolean;
    locale?: Locale;
}) { return <ol className={detailed ? 'roadmap detailed' : 'roadmap'}>{t(milestones.map(m => <li className={m.current ? 'milestone current' : 'milestone'} key={m.version}><div className="milestone-version"><span className="milestone-dot"/>{t(m.version, locale)}</div><div className="milestone-content"><span className={m.current ? 'tag' : 'tag planned'}>{t(m.current ? 'Obecny MVP · w rozwoju' : 'Planowane', locale)}</span><h3>{t(m.name, locale)}</h3><p>{t(m.features, locale)}</p>{t(detailed && <p className="milestone-description">{t(m.description, locale)}</p>, locale)}</div></li>), locale)}</ol>; }
