import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Architektura', 'Architektura HomeIntelCore: Next.js, Core, model obiektów, PostgreSQL, kolejka zadań, DocumentProvider i Paperless-ngx. Home Assistant, monitoring, energia i lokalne AI jako integracje planowane, WireGuard jako planowana warstwa zdalnego dostępu.', '/architecture/');
import View from '@/components/views/architecture';
export default function Page(){return <View locale="pl"/>}
