import { pageMetadata } from '@/lib/site';
export const metadata = pageMetadata('Architektura systemu i Object Engine', 'Architektura HomeIntelCore: interfejs, Core, model obiektów i relacji, PostgreSQL, kolejka zadań i Paperless-ngx. Home Assistant, monitoring, energia, lokalne AI i WireGuard jako warstwy planowane.', '/architecture/', "en");
import View from '@/components/views/architecture';
export default function Page(){return <View locale="en"/>}
